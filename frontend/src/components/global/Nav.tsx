import React from 'react';

const Nav = () => {
  return (
    <nav>
      {/* Logo Left */}
      <div>Logo</div>

      {/* Twitter Login Button or User Info */}
      <div>
        {/* Conditional: Show "Continue with Twitter" or Profile */}
      </div>
    </nav>
  );
};

export default Nav;
